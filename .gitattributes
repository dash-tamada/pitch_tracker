# Keep LF line endings in the repository and working copy on every OS.
# Migration files are hashed by content, and CI runs on Linux.
* text=auto eol=lf
*.png binary
*.jpg binary
*.webp binary
*.pdf binary
